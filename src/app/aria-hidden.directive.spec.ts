import { AriaHiddenDirective } from './aria-hidden.directive';

describe('AriaHiddenDirective', () => {
  it('should create an instance', () => {
    const directive = new AriaHiddenDirective();
    expect(directive).toBeTruthy();
  });
});
